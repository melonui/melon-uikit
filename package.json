{
  "name": "@melonui/compose",
  "version": "1.0.1",
  "scripts": {
    "test": "test",
    "build": "lerna run --scope @melonui/* --parallel build",
    "publish": "npm run build && lerna publish"
  },
  "dependencies": {
    "react": "^17.0.2",
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "@types/react": "^17.0.38",
    "lerna": "^4.0.0"
  }
}
